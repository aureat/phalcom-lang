name: Module lifecycle probe

on:
  push:
    branches: [codex/lsp-single-world-retirement]

permissions:
  contents: write

concurrency:
  group: module-lifecycle-probe-${{ github.ref }}
  cancel-in-progress: true

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/lsp-single-world-retirement
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Confirm standalone lifecycle
        run: cargo test -p phalcom-modules --test standalone_incremental_imports -- --nocapture
      - name: Probe semantic import query provenance
        run: cargo test -p phalcom-semantic --test semantic module_query_probe -- --nocapture
