# Build artifacts
/target

# OS / editor cruft
.DS_Store
.idea/
.vscode/
*.iml
*.swp

# Per-machine skill lockfile (not shared across dev environments)
skills-lock.json

# Local dev env (non-standard root config; prefer .cargo/config.toml if needed)
/config.toml

# Generated files
*.log
.graphify_version
# graphify code knowledge graph (generated; rebuilt locally via post-commit hook)
/graphify-out

# git worktrees used by agents
.claude/worktrees/

# REPL history
.phalcom_history
