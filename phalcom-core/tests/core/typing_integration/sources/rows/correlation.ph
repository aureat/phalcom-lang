class RowCorrelationProbe {
    @class
    compatible() {
        let result = RowCalculus.sameRemainder(
            #{ id: 1, label: "left" },
            #{ id: 2, label: "right" }
        )
        result
    }
}
