let order = List.new()
const getLeft = || {
  order.append("L")
  2
}
const getRight = || {
  order.append("R")
  [1, 2, 3]
}
let res = getLeft.call() in getRight.call()
System.print(order.join(""))
System.print(res)
