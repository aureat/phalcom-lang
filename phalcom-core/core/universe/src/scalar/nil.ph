//! Private sentinel class presentation. `Nil` is not exported through the
//! scalar package; it exists so the source corpus records the runtime class
//! behind the private tagged value.
@native
class Nil is Object {}
