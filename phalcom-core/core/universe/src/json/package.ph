@!documentation("JSON serialization and deserialization.")

let parse = |text| {
    // Standard JSON parser entry point
    text
}

let stringify = |val| {
    // Standard JSON stringifier entry point
    val
}

export parse
export stringify
