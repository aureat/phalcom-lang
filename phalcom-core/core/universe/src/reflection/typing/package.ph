@!documentation("Runtime type reflection and capability-scoped metadata queries.")

expose .kind
expose .type_descriptor
expose .type_parameter
expose .generic_signature
expose .signature
expose .type_use
expose .result
expose .evidence
expose .context
