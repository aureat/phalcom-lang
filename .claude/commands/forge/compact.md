---
description: Write a lossless continuation seed (forge-state.md or docs/forge/STATE.md), emit a ≤10-line digest, and tell the user to run the built-in /compact — trims this thread while keeping the thread.
---

Invoke the `forge` skill (Skill tool, `skill: "forge"`) and execute **§Compact**.

Do not narrate the dispatch. Same extraction as Handoff (current context only, no survey): write the seed, emit the short digest, then instruct the user to run the built-in `/compact` now and resume from the seed.
