---
description: Run the Phalcom implementation pipeline — spec-anchored, adversarially-verified stabilize/audit/verify/plan/implement/review as a continuous no-idle parallel fleet (ready-queue scheduling, worktree isolation, per-unit integration).
---

Invoke the `forge` skill (Skill tool, `skill: "forge"`) and execute **§Orchestrate**.

Do not narrate the dispatch. This is §Senior applied to the pipeline — obey the ladder, ledger, stop-rule, subagent contract, and re-entry rules while running it. Pick the correct slice (review / implement one unit / stabilize / plan the whole thing) from the user's request.
