---
description: Print the forge work ledger — `foreground:` (your queued tasks) and `outstanding:` (subagent → deliverable / write-set). Nothing else.
---

Invoke the `forge` skill (Skill tool, `skill: "forge"`) and execute **§Status**.

Do not narrate the dispatch. Print `foreground:` and `outstanding:` only. If unknown, reconstruct from the last few turns — do not survey.
