### Handoff Prompt 1 — Continue the Semantic Correctness Program, Then Semantic Completeness

# Phalcom Semantic Analyzer — Program Continuation Handoff

You are taking over an ongoing, repository-grounded compiler/type-system architecture program for the programming language **Phalcom**.

Your responsibility is to continue the **Semantic Correctness Program**, complete its remaining eight program documents/slices, and then prepare the transition into the separate **Semantic Completeness Program**.

This is not a greenfield design exercise.

The repository implementation, the canonical semantic specifications, and the already-completed correctness work establish a large amount of architecture that must be preserved.

Do not begin by writing a new specification.

First perform fresh repository archaeology against the current `main` branch of:

```text
aureat/phalcom-lang
```

The repository is the implementation source of truth.

---

# 1. Work already completed

The first two semantic-correctness technical slices have already been designed.

## Technical Specification 01

Topic:

```text
Formal Knowledge, Required-Premise Composition,
and Expression-Result Integrity
```

It establishes the implementation model for:

- canonical required-knowledge composition;
- `Established` / `Assumed` authority preservation;
- exact `UnknownReason` preservation;
- exact `DynamicReason` preservation;
- required-expression dependency propagation;
- causal/status coherence;
- `Invalid(C) => causal invalidity contains C`;
- atomic expression-product publication;
- aggregate literal correctness;
- tuple/record/list/set/map premise completeness;
- expansion fail-closed behavior;
- tuple-pattern decomposition preservation;
- `for` iterable knowledge/causal preservation.

There is also a corresponding detailed implementation plan.

Do not redesign this slice unless repository verification proves a concrete problem.

## Technical Specification 02

Topic:

```text
Canonical Callable Application and Operation Semantics
```

It establishes one canonical semantic application architecture for:

- explicit method calls;
- implicit-self calls;
- callable-valued locals;
- binary operators;
- unary operators;
- getter callables;
- setter callables;
- subscript getters;
- subscript setters;
- constructors;
- native callables;
- iteration-protocol callable boundaries.

Its core architectural rule is:

```text
syntax may select a callable target;
syntax may not select a weaker callable-application algorithm
```

The specification introduces concepts equivalent to:

```rust
CallableApplicationTarget
CallTargetAuthority
CallPremise
ApplicationArgument
StaticCallShape
ArgumentBindingPlan
apply_resolved_callable(...)
apply_generic_callable(...)
```

and eliminates semantic fast paths such as:

```text
resolve signature
→ read return type
→ publish result
```

without argument relation checking.

There is also a detailed implementation plan containing 15 execution tasks.

Do not duplicate this architecture in later work.

---

# 2. First action: discover the canonical program ledger

There are eight remaining Semantic Correctness program slices after the first two completed slices.

Do not blindly infer their numbering or titles from this handoff.

Find the canonical Semantic Correctness program charter / ledger / index in the repository and establish:

```text
exact remaining document order
exact names
exact intended scope
dependencies between documents
correctness-program completion criteria
```

If older plans/specs disagree with the canonical ledger, report the disagreement and use the canonical current source.

Known remaining architectural domains include, at minimum:

```text
generic inference and proof integrity
generic substitution / specialization / relation integrity
canonical source/formal identity
advisory authority and resolution safety
module/workspace transactional correctness
semantic-session publication and incrementality
whole-program semantic-correctness verification and closure
```

There may be an additional independently named program slice in the current canonical ledger. Discover it rather than inventing it.

---

# 3. Required repository archaeology

Before producing the next specification, inspect the current implementation deeply.

At minimum inspect:

```text
phalcom-semantic/src/
phalcom-semantic/tests/
phalcom-lsp/src/
docs/spec/semantic-analyzer/
docs/impl/semantic/
docs/adr/
```

and any relevant core/parser code required to validate syntax or runtime semantics.

For every upcoming slice:

1. inspect repository tree;
2. inspect implementation files, not filenames alone;
3. follow symbols and call paths;
4. inspect relevant tests;
5. inspect canonical semantic specifications;
6. inspect recent commits if architecture may have changed;
7. compare old implementation plans against current code;
8. identify already-landed repairs;
9. identify stale assumptions;
10. distinguish current implementation defects from completeness gaps.

The implementation is the source of truth for current state.

The canonical semantic specifications are the source of truth for intended semantics where explicitly normative.

Older implementation-status documents may be stale.

---

# 4. Central semantic philosophy

The following principles are already established and should be treated as architectural constraints unless current canonical specs explicitly supersede them.

## 4.1 Checker authority

When the semantic checker can formally establish a fact, that fact is authoritative.

Example:

```text
checker proves CellNum
developer annotates Int
```

The annotation is wrong.

Do not reconcile this by trusting both equally.

Developer annotations become assumptive evidence only when the checker cannot independently establish the proposition and the unknown reason permits a contract assumption.

---

## 4.2 Epistemic states

Phalcom distinguishes:

```text
Established
Assumed
Unknown(reason)
Dynamic(reason)
```

These states are semantically meaningful.

A concrete `TypeId` is not sufficient to describe semantic knowledge.

Never silently perform:

```text
Assumed -> Established
Unknown -> Known
Dynamic -> Known
```

without a valid proof rule.

---

## 4.3 Causal invalidity is orthogonal to type knowledge

The analyzer may correctly publish:

```text
Established(Int)
+
Invalid(C1)
```

or:

```text
Established(Int)
+
Ready
+
non-clean upstream causal invalidity
```

depending on whether the current proposition itself was refuted.

Do not collapse type knowledge and validity into one axis.

---

## 4.4 Required-premise completeness

A conclusion may only be formally established when every premise required to establish that proposition has been accounted for.

Do not use patterns such as:

```rust
if let Some(ty) = child.knowledge.ty() {
    inputs.push(ty);
}
```

when a missing child is a required premise.

---

## 4.5 Identity preservation

Semantic identity should eventually be explicit and canonical.

Do not reconstruct identity from lossy tuples such as:

```text
(name, source range, owner)
```

if canonical IDs already exist or can be propagated.

---

## 4.6 Authority monotonicity

Information from a weaker authority source must not become stronger merely by flowing through another semantic operation.

Examples:

```text
Assumed callable
→ must not produce Established fixed return

Assumed receiver
→ must not establish target-dependent fixed result
```

unless there is a separate independent proof.

---

## 4.7 Atomic publication

A semantic product should not be partially committed and then repaired piecemeal.

This applies both to expression products and later to:

```text
workspace state
semantic session state
published snapshots
incremental products
```

---

# 5. Correctness versus completeness

This distinction is critical.

The **Semantic Correctness Program** is not intended to make the analyzer understand every valid Phalcom program.

It is intended to guarantee:

> Whenever the analyzer publishes a semantic claim, that claim obeys the formal semantic laws of the language.

Correctness work therefore prefers:

```text
Unknown
Dynamic
Blocked
Suppressed
```

over an unjustified precise answer.

Examples of correctness bugs:

```text
[1, unresolved] -> Established List<Int>

1 + "hello" -> Ready Int without checking RHS

list["wrong"] -> Ready element type without checking index

Assumed callable -> Established return

workspace update fails after mutating committed internal state

advisory lookup bypasses export visibility
```

The **Semantic Completeness Program** begins after correctness closure.

Completeness asks:

> Which programs that the language semantics make statically knowable are still reported as Unknown, Dynamic, Blocked, unresolved, or unsupported?

Completeness work may include:

- deeper callable-pack analysis;
- complete rest/spread parameter binding;
- richer iterable/cursor protocol inference;
- recursive/fixpoint inference;
- more advanced generic inference;
- higher-kinded inference coverage;
- GADT/ADT reasoning;
- exhaustiveness;
- pattern refinements;
- refined types;
- stronger branch narrowing;
- richer structural relations;
- more complete public-signature inference;
- additional protocol/trait reasoning;
- improved LSP query completeness.

Do not solve completeness by weakening correctness invariants.

Correctness is the foundation on which completeness is built.

---

# 6. Remaining Semantic Correctness deliverables

For each of the remaining eight program slices, produce two artifacts:

```text
1. Technical Specification
2. Corresponding Implementation Plan
```

The technical specification explains:

```text
what architecture and semantics must exist
why
what invariants it establishes
what current code violates
what the correct end state is
```

The implementation plan explains:

```text
exactly what the implementing agent does
file by file
test by test
commit by commit
```

Do not blur specification and implementation plan.

---

# 7. Technical specification quality bar

Each technical specification must contain:

```text
purpose
scope
non-goals
repository baseline
verified current state
normative semantic basis
identified correctness defects
architectural decision
new/modified types and APIs
exact semantic laws
migration rules
forbidden implementation patterns
test requirements
acceptance gates
handoff to the next slice
```

For each major repository claim provide:

```text
exact file path
symbol/function/type
observed behavior
why it is correct or incorrect
```

Do not invent implementation details from filenames.

---

# 8. Implementation plan quality bar

After each technical specification is complete, produce a separate implementation plan.

The plan must be actionable by an agent with little context.

Every task should contain:

```text
exact files
exact interfaces consumed
exact interfaces produced
RED test
test command
implementation action
concrete code or algorithm
GREEN verification
commit boundary
```

Use small independently reviewable tasks.

Plans must explicitly say things such as:

```text
Modify file X.

Find function Y.

Delete branch Z.

Replace it with this algorithm.

Add this type.

Add this test.

Run this command.

Expected result: FAIL/PASS.

Commit with this boundary.
```

Avoid placeholders such as:

```text
TODO
TBD
handle edge cases
add appropriate tests
implement the above
```

---

# 9. Likely next architectural slice: generic inference and proof integrity

Unless the canonical ledger says otherwise, the next technical slice should concern generic inference proof integrity.

Repository archaeology should investigate, at minimum:

```text
checker/call.rs
checker/inference.rs
types/constraint.rs
types/substitution.rs
types/outcome.rs
types/evidence.rs
generic semantic tests
bidirectional call tests
```

Known issues requiring fresh verification include:

### Unknown generic premise omission

Current/previously observed shape:

```text
if argument has TypeId:
    add inference constraint
else:
    contribute nothing
```

This can make:

```phalcom
first<T>(1, unresolved)
```

solve `T = Int` and publish an unjustifiably strong result.

The correct model must distinguish:

```text
substitution solvable
```

from:

```text
call proposition fully proven
```

### Generic result authority

Generic result authority must depend on all premises actually used to establish the substitution/result proposition.

Likely authority relationship:

```text
result authority
=
meet(
    receiver/callee authority,
    callable contract authority,
    substitution support,
    result-contract authority,
    all required generic premises
)
```

Do not ratify this formula without fresh repository and spec verification.

### Expected-result constraints

Expected context may constrain inference.

Expected context must not become evidence that the runtime value actually has that type unless the semantic proof rules permit it.

### Generic solver outcome

Preserve structured states:

```text
solved
underconstrained
conflicting
blocked
dynamic
cancelled
budget exceeded
internal failure
```

Do not reduce generic proof state to `Option<TypeId>`.

---

# 10. Generic specialization slice

A later slice should verify receiver/class substitution through:

```text
method parameters
return types
method generic bounds
where clauses
higher-kinded/type-lambda structures
```

Known previously observed defect requiring fresh verification:

```phalcom
Box<T>.convert<U>(U) -> U
where U <: T
```

on:

```text
Box<Number>
```

may specialize parameters/return while leaving enclosing `T` unspecialized inside method generic constraints.

Do not assume this remains true—inspect current source first.

---

# 11. Canonical identity slice

Investigate whether semantic/source identity is reconstructed from:

```text
name
range
owner
selector fragments
```

rather than propagated canonically.

This slice should address, where verified:

```text
SourceSiteId propagation
formal declaration identity
callable implementation identity
constructor public identity vs implementation identity
source occurrence indexing
definition/reference linkage
```

Do not combine this with advisory lookup policy.

---

# 12. Advisory authority slice

Advisory/LSP inference must never outrank the compiler's canonical semantic authority.

Freshly investigate:

```text
module exports
member visibility
class-object dispatch
instance-side fallback
constructor mapping
advisory resolution
hover/go-to-definition occurrence resolution
```

Previously observed candidates requiring verification:

```text
module advisory resolution falling back to dispatch surfaces after export lookup

class-object lookup falling generically back to instance side
```

The target invariant is:

> Advisory machinery may consume canonical compiler semantics, but it may not invent broader authority than the compiler.

---

# 13. Transactional correctness slice

Investigate workspace/module mutation paths.

Verify whether operations such as:

```text
WorkspaceModuleSession::apply()
set_workspace_roots()
set_overlay_batch()
```

can mutate committed state before a fallible rebuild completes.

The required end-state law is:

```text
failed update
=> no externally observable committed-state mutation
```

Use:

```text
candidate state
build/validate
commit atomically
```

rather than mutate-and-roll-back where practical.

---

# 14. Semantic-session publication and incrementality slice

Investigate:

```text
SemanticWorkspaceSession::update()
published semantic snapshots
generation counters
dependency fingerprints
filesystem/store/session mutation
incremental invalidation
```

The law is stronger than “return the old snapshot on error.”

It should be:

```text
failed candidate update
=> no hidden semantic state advances either
```

Also verify whether invalidation/publication decisions use real semantic-product fingerprints or merely source-change proxies.

---

# 15. Correctness closure slice

The final correctness slice should not add another major subsystem.

It should prove the program's invariants across the repository.

Create:

```text
adversarial cross-feature regressions
metamorphic tests
product invariant scans
authority monotonicity tests
identity preservation tests
transaction atomicity tests
incremental equivalence tests
```

Examples:

```text
incremental result == clean rebuild result

operator application == equivalent explicit send semantics

setter assignment operation == direct setter call operation
except source assignment result is Unit

same semantic fact queried through compiler/LSP/advisory layers
must preserve canonical identity and authority
```

Perform repository-wide structural searches for forbidden patterns introduced by older code.

The final correctness document should establish an explicit gate for starting Semantic Completeness.

---

# 16. Semantic Correctness completion definition

The program is complete only when the analyzer can credibly claim:

```text
1. no known proposition is established from missing required evidence;
2. assumptions never silently become proofs;
3. Unknown and Dynamic states are preserved honestly;
4. invalidity causes are coherent and bounded;
5. call-like syntax uses canonical semantic application;
6. generic inference distinguishes solvability from proof;
7. specialization preserves all relevant generic structure;
8. semantic identity is canonical rather than reconstructed;
9. advisory/LSP layers do not outrank compiler authority;
10. failed workspace/session updates are transactional;
11. published incremental semantics are equivalent to clean semantics;
12. regression infrastructure checks these laws continuously.
```

Only after this gate should the **Semantic Completeness Program** become the primary semantic initiative.

---

# 17. Semantic Completeness follow-on

After Semantic Correctness closure, produce a new program charter for Semantic Completeness.

Do not immediately implement completeness features.

First create an inventory of all remaining:

```text
UnknownReason
DynamicReason
Blocked paths
unsupported syntax/semantic cases
fallback structural shortcuts
unimplemented protocol relations
missing generic cases
LSP query gaps
```

Classify each by:

```text
soundly unknowable
deliberate dynamic boundary
implementation incompleteness
language-design decision pending
performance/budget limitation
parser/runtime capability missing
```

Then prioritize completeness work by developer impact and architectural leverage.

The completeness program must inherit all correctness invariants.

No completeness improvement may achieve precision by laundering authority.

---

# 18. Working process

For each remaining Semantic Correctness slice:

1. inspect repository;
2. report significant implementation discoveries;
3. compare current code with canonical semantics;
4. design the technical specification;
5. self-review for architectural consistency;
6. produce the technical specification;
7. then produce its implementation plan;
8. do not begin the following slice until dependencies are explicit.

When repository evidence contradicts an older handoff assumption, trust current inspected source and report the correction.

Avoid shallow compiler advice.

Avoid aspirational APIs unsupported by current architecture.

Avoid speculative rewrites.

The intended result is a repository-grounded engineering program that another agent can execute without re-deriving the architecture from scratch.

Begin by locating the canonical Semantic Correctness program ledger and reporting the exact remaining eight slices in their canonical order.
```

### Handoff Prompt 2 — Independent Adversarial Verification of the Specs and Plans

```markdown
# Phalcom Semantic Correctness — Independent Specification and Implementation-Plan Verification

You are acting as an independent senior compiler engineer and architecture reviewer.

Your job is **not** to continue the existing design.

Your job is to verify it adversarially.

Assume the previous agent may have:

- misunderstood the repository;
- relied on stale implementation observations;
- invented APIs;
- missed existing implementation;
- proposed syntax the parser does not accept;
- violated current ownership boundaries;
- confused correctness with completeness;
- duplicated existing semantic machinery;
- made an implementation plan that cannot actually be executed.

Do not defer to the previous agent's conclusions.

Re-derive the relevant architecture from the current repository.

Repository:

```text
aureat/phalcom-lang
```

Use the current default branch unless explicitly instructed otherwise.

---

# 1. Artifacts under review

Locate and review the completed Semantic Correctness artifacts for the first two slices.

They should include technical specifications and corresponding implementation plans concerning approximately:

## Slice 01

```text
Formal Knowledge, Required-Premise Composition,
and Expression-Result Integrity
```

## Slice 02

```text
Canonical Callable Application and Operation Semantics
```

Do not assume filenames from this handoff are exact.

Search the repository and/or supplied files for the canonical copies.

Also read the Semantic Correctness program charter/ledger and the relevant canonical semantic-analyzer specifications.

---

# 2. Review objective

Determine whether these documents are:

```text
repository-grounded
semantically sound
internally consistent
implementable
correctly scoped
compatible with current Phalcom architecture
sufficiently tested
properly separated from completeness work
```

The correct output is not praise or a summary.

The correct output is an engineering audit.

---

# 3. Mandatory fresh repository archaeology

Before evaluating the documents, inspect current source.

At minimum inspect:

```text
phalcom-semantic/src/checker/
phalcom-semantic/src/types/
phalcom-semantic/src/dispatch.rs
phalcom-semantic/src/surface/
phalcom-semantic/src/source_index/
phalcom-semantic/src/workspace/
phalcom-semantic/tests/
phalcom-ast/
phalcom-core/
docs/spec/semantic-analyzer/
docs/spec/callables/
docs/spec/collections/
docs/adr/
```

where relevant.

Inspect implementations, not only documentation.

Follow important symbols to their definitions and consumers.

Inspect relevant tests.

Inspect recent commits where the architecture may have changed.

Do not infer semantics from filenames.

---

# 4. Verify repository baseline and staleness

Determine:

```text
current HEAD
commit dates
whether the specs' stated baseline is still current
whether source changed after the documents were written
```

For every source-level claim in a spec or plan, classify it as:

```text
Verified current
Already fixed
Partially true
Stale
Incorrect
Unverifiable
```

If the implementation has already moved beyond a proposed task, say so.

Do not recommend reintroducing old architecture merely because a plan describes it.

---

# 5. Verify Technical Specification 01

Check every major premise.

In particular verify:

## Evidence model

Does current source actually represent:

```text
TypeKnowledge
TypeEvidence
EvidenceStatus
EvidenceOrigin
UnknownReason
DynamicReason
```

as assumed by the spec?

Are fields/private APIs described correctly?

Does `map_type()` preserve the claimed dimensions?

## Knowledge composition

Verify all aggregate literal paths:

```text
List
Set
Map
Tuple
Record
spread/expansion
```

Determine whether any required child still disappears through:

```rust
if let Some(ty) = knowledge.ty()
```

or equivalent filtering.

Verify the proposed distinction between:

```text
reachable-flow join
required-premise composition
```

is semantically justified and matches current architecture.

## Status/causal coherence

Verify:

```text
AnalysisStatus
CausalInvalidity
diagnostic ownership
ExpressionAnalysis
TypedExpression
```

and whether:

```text
Invalid(C) => causal contains C
```

is already guaranteed, partially guaranteed, or violated.

Check whether the plan proposes APIs that duplicate existing ones.

## Expression publication

Inspect the actual `analyze_expression` publication path.

Determine whether expression products are currently:

```text
atomic
partially published
mutated later
```

and whether the plan's proposed publication helper is necessary.

## Binding mismatch

Verify the exact current binding-initializer mismatch behavior.

Confirm whether status and causal-invalidity can disagree.

## Pattern decomposition and iteration

Verify:

```text
tuple pattern decomposition
UnknownReason preservation
DynamicReason preservation
for-loop iterable analysis
causal propagation into pattern bindings
```

Do not trust prior claims without source evidence.

---

# 6. Verify Technical Specification 02

This review must be especially rigorous because it proposes a major semantic convergence.

## Existing call architecture

Inspect:

```text
checker/call.rs
checker/expression.rs
checker/context.rs
dispatch.rs
```

Trace ordinary method application end to end.

Confirm exactly which layers currently own:

```text
selector construction
dispatch
argument mapping
contextual argument typing
relation checking
generic inference
fixed return derivation
status
causality
callable identity
explanations
```

## Binary operators

Verify whether binary operators currently check RHS against the selected callable parameter.

Test or source-trace:

```phalcom
1 + "hello"
```

Do not assume the claimed bug still exists.

## Unary operators

Determine whether migrating unary operators through canonical call application is semantically necessary or merely architectural cleanup.

## Getter/setter behavior

Verify:

```text
direct field access
getter callable access
direct field write
setter callable write
```

Check whether assignment expression value is currently RHS or Unit.

Confirm the language's canonical assignment rule from actual specification/runtime/compiler behavior.

## Subscripts

Trace:

```text
List<T> get
Map<K,V> get
List<T> set
user-defined subscript get
user-defined subscript set
```

Verify which paths are structural shortcuts and which already dispatch through canonical surfaces.

Check whether index/key relations are currently skipped.

## Callable-valued locals

Verify whether an assumed callable value currently creates an Established synthetic return.

Verify whether:

```phalcom
let x = 1
x()
```

can degrade to a value read or fall through incorrectly.

## Dispatch failure

Verify whether child call arguments are analyzed when dispatch fails.

## Spread/dynamic labels

Inspect AST representation and selector construction.

Verify whether spread is currently approximated as one positional slot.

Check the proposed `ApplicationArgument` shapes against the actual AST enum.

## Assignment `Unit`

Verify against:

```text
language spec
compiler lowering
runtime behavior
existing tests
```

Do not accept the technical spec's assertion solely because it says so.

## Constructor/native origins

Verify actual:

```text
CallableSemanticKind
constructor Self specialization
EvidenceOrigin
```

and whether the proposed origin rules match current architecture.

---

# 7. Verify every proposed API

For every newly proposed type/function in the technical specifications and implementation plans, classify:

```text
Necessary
Reasonable but naming questionable
Duplicates existing abstraction
Wrong layer
Cannot compile with current types
Requires an unmentioned prerequisite
Over-scoped
Under-specified
```

Examples to scrutinize include concepts equivalent to:

```rust
compose_required_knowledge(...)
propagate_required_dependencies(...)

CallableApplicationTarget
CallTargetAuthority
CallPremise
ApplicationArgument
StaticCallShape
ArgumentBindingPlan

resolve_dispatch_target(...)
apply_resolved_callable(...)
apply_non_generic_callable(...)
apply_generic_callable(...)
analyze_unresolved_application(...)
assignment_result_from_call(...)
```

Check ownership boundaries.

Ask:

```text
Should this live in call.rs?
context.rs?
composition.rs?
types/evidence.rs?
a new module?
```

Do not accept the previous file placement automatically.

---

# 8. Verify exact Rust feasibility

Implementation plans contain concrete Rust sketches.

Inspect them as code.

Identify:

- incorrect type names;
- incorrect lifetimes;
- impossible borrows;
- private-field access;
- nonexistent constructors;
- wrong enum variants;
- wrong method signatures;
- incorrect selector APIs;
- misuse of `Copy` versus `Clone`;
- mutable-borrow conflicts involving `ctx.store`;
- AST variants that do not exist;
- tests that cannot parse.

Where code sketches are illustrative rather than directly compilable, explicitly say so.

Distinguish:

```text
architecturally correct pseudocode
```

from:

```text
copy/paste-compilable implementation
```

The plans should not claim the latter if they are only the former.

---

# 9. Verify test syntax

For every proposed Phalcom regression fixture, verify against the actual parser.

Examples requiring explicit validation:

```phalcom
value=(put next: Int) { ... }

[_ key: Int]=(put value: Int) { ... }

run(worker: Worker) { ... }

class Probe {
  value: Int = 0
}

Map<String, Int>

receiver.target(*values)

CellNum.new("wrong")
```

Do not assume older syntax still parses.

Where a proposed fixture is stale, provide the exact current syntax.

---

# 10. Verify semantic test harness assumptions

Inspect:

```text
phalcom-semantic/tests/semantic/support/fixture.rs
```

Check whether the plans correctly use:

```rust
Fixture::new
fixture.callable(...)
fixture.expression(...)
fixture.callable_id(...)
fixture.ty(...)
fixture.assert_diagnostic(...)
```

Verify source range matching behavior.

Determine whether expression strings such as:

```text
self.value = 1
values[0] = "wrong"
```

are actually published as expression analyses with exactly those ranges.

If not, propose the correct locator strategy.

---

# 11. Verify correctness/completeness separation

For every proposed change classify it:

```text
Semantic correctness
Semantic completeness
Language-design change
Performance optimization
LSP/advisory enhancement
Refactor only
```

Flag any task placed in the correctness program that actually broadens semantic coverage rather than repairing false claims.

A correctness change should generally transform:

```text
wrong precise answer
```

into:

```text
correct precise answer
or
honest Unknown/Dynamic/Blocked
```

It should not introduce unrelated new inference capability.

---

# 12. Verify interaction between Part 01 and Part 02

Part 02 is supposed to build on Part 01.

Check for duplication.

Examples:

- Does Part 02 define another status-precedence algorithm rather than reuse Part 01?
- Does Part 02 manually join causal invalidity in syntax wrappers despite Part 01 dependency propagation?
- Does `CallCheckResult -> TypedExpression` preserve Part 01 product invariants?
- Can a call become `Invalid(C)` without C entering causal invalidity?
- Can unresolved call handling accidentally suppress a causally-invalid-but-known receiver?
- Does assignment projection preserve a non-Ready underlying operation without corrupting Unit knowledge?

Flag every duplicated semantic law.

---

# 13. Verify generic boundary discipline

Technical Specification 02 intentionally defers most generic-proof work.

Check whether its implementation plan nevertheless accidentally changes:

```text
generic constraint collection
solver support rules
expected-result inference authority
unknown generic argument treatment
where-clause semantics
receiver generic specialization
```

If so, identify the spillover.

Likewise verify whether Part 02 leaves the generic engine behind a sufficiently clean canonical seam for Part 03.

---

# 14. Verify semantic laws

Attempt to falsify these proposed laws with actual repository behavior:

```text
Epistemic monotonicity
Required-premise completeness
Cause coherence
Identity preservation
Authority monotonicity
Atomic publication
Canonical application
```

For each law provide:

```text
current satisfying examples
current violations
proposed repair
remaining counterexamples
```

Do not merely restate the law.

---

# 15. Required adversarial examples

Construct or source-trace at least these examples where current parser permits them:

```phalcom
[1, missing]

(1, missing)

#{ value: missing }

1 + "hello"

list["wrong"]

map[wrongKey]

list["wrong"] = 1

list[0] = "wrong"

object.property = "wrong"

let x = 1
x()

assumedCallable(1)

knownReceiver.missing(missingArgument)

constructorWithWrongArgument(...)

genericCall(known, unresolved)
```

For each, report:

```text
current TypeKnowledge
EvidenceStatus
EvidenceOrigin
AnalysisStatus
CausalInvalidity
diagnostics
callable identity
```

where available.

This is the most important empirical part of the review.

---

# 16. Verify implementation-plan sequencing

Review every task boundary.

For each task ask:

```text
Can this compile independently?
Can this be reviewed independently?
Does its RED test actually fail before the change?
Does the proposed implementation make only that test green?
Does it rely on an API introduced in a later task?
Does the stated commit contain all required files?
```

Flag sequencing bugs.

Especially inspect whether:

```text
Part 02 Task 5 canonical engine
```

depends on status helpers or Part 01 APIs that are not actually present yet.

---

# 17. Verify structural searches and acceptance gates

Run or conceptually validate the plans' proposed `rg` audits.

Improve them where they would produce false positives or miss equivalent code.

Examples:

```bash
rg 'promote_exact_return'
rg 'sig\.return_type'
rg 'PackItem::Expand'
rg 'parameters\.first'
rg 'resolve_call\('
rg 'apply_resolved_callable\('
```

An acceptance gate should detect semantic bypasses, not merely a particular function name.

Recommend stronger structural checks where needed.

---

# 18. Output format

Produce a detailed verification report with these sections.

## A. Executive verdict

One of:

```text
APPROVE
APPROVE WITH REQUIRED CORRECTIONS
REVISE BEFORE IMPLEMENTATION
ARCHITECTURALLY UNSOUND
```

Give concise reasons.

## B. Repository baseline

State:

```text
HEAD
relevant recent commits
documents inspected
source modules inspected
tests inspected
```

## C. Technical Specification 01 findings

For each major section:

```text
Verdict
Repository evidence
Issue
Required correction
```

## D. Part 01 implementation-plan findings

Review:
- task order;
- API correctness;
- test feasibility;
- missing actions;
- stale actions.

## E. Technical Specification 02 findings

Same structure, with especially deep analysis of call unification.

## F. Part 02 implementation-plan findings

Audit all task sequencing and concrete code.

## G. Cross-spec contradictions

Identify any disagreement between:

```text
Part 01
Part 02
canonical semantic specs
current implementation
runtime/compiler behavior
```

## H. Correctness/completeness boundary violations

List anything in the wrong program.

## I. Missing correctness defects

Identify repository-grounded correctness issues not covered by the two documents.

Do not broaden into general feature requests.

## J. Required edits before implementation

Provide a numbered, actionable correction list.

Use:

```text
Document:
Section:
Current text/assumption:
Problem:
Replacement:
Repository evidence:
```

## K. Final implementation readiness matrix

For every task:

```text
Ready
Needs correction
Blocked by Part 01
Blocked by repository drift
Should be removed
Should be split
```

---

# 19. Review discipline

Do not modify repository code during the initial audit.

Do not rewrite the documents before presenting findings.

Do not assume previous conclusions are correct.

Do not criticize merely because you prefer a different architecture.

A finding must be based on one of:

```text
repository contradiction
semantic contradiction
unimplementable API
incorrect parser/runtime assumption
test infeasibility
ownership violation
correctness/completeness scope error
```

Where the existing design is correct, say so and explain why.

The purpose is to make these documents safe to hand to an implementation agent.

Begin with fresh repository archaeology, not with the existing specifications.