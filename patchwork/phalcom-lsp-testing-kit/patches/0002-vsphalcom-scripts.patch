# Apply after:
#   cd tools/vsphalcom
#   npm install --save-dev @vscode/vsce
#
# This patch only adds scripts; npm should own package-lock.json changes.

diff --git a/tools/vsphalcom/package.json b/tools/vsphalcom/package.json
--- a/tools/vsphalcom/package.json
+++ b/tools/vsphalcom/package.json
@@
-    "test": "node ./out/test/runTest.js"
+    "test": "node ./out/test/runTest.js",
+    "test:lsp:e2e": "cargo build --manifest-path ../../Cargo.toml -p phalcom-lsp && npm test",
+    "vsix": "npm run package && vsce package"
   },
