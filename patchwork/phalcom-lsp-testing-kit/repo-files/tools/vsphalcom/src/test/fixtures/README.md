# VS Code E2E fixture notes

The E2E suite creates mutable files in a temporary directory so tests never dirty the repository.

`live-semantic.ph` is retained as a manual/reference fixture for extension-host debugging.
